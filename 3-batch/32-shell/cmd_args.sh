#!/bin/bash

echo '---------------------这是"$*"的输出---------------------------'
for arg in "$*"
  do
    echo $arg
  done

echo '-----------------------以下为$@的输出，参数列表打印---------------------------'
for arg in "$@"
  do
    echo $arg
  done
